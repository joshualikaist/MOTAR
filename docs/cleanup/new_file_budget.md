# New-file budget (2026-09-24)

Every tracked file that exists after the reorganisation and did not exist at the base commit `7c40d5d`.
Moves done with `git mv` are not new files. No tracked file was deleted.

| | Tracked files | Net change |
| --- | ---: | ---: |
| Base `7c40d5d` | 5,365 | |
| After the first pass, as staged before the review | 5,409 | +44 |
| After the second pass: cleanup set A | 5,392 | **+27** |
| With research set B (replication instrumentation and preregistration) | 5,397 | +32 |

The second pass removed:
- 14 PNG and PDF renders, which are now rebuilt on demand;
- 6 maintenance notes, merged into 3;
- 2 verbatim archive copies, recoverable with `git show 7fb547d:…`;
- 1 extra archive README.

It added the test-profile runner and the D8c audit.

## Set A — cleanup, claim audit, D8c audit (27 files)

| Path | Why it is tracked | Source or generated | Can it be regenerated? | Needed in Git? |
| --- | --- | --- | --- | --- |
| `PROJECT.md` | Canonical: purpose, questions, scope, phase | Source | No | Yes |
| `ARCHITECTURE.md` | Canonical: how the system connects; directory table; policies F and R | Source | No | Yes |
| `DESIGN_SYSTEM.md` | Canonical: site and figure tokens that tests bind to the CSS and the generator | Source | No | Yes |
| `docs/EVIDENCE.md` | Canonical: every result with its interval, its unit of inference and its record | Source (test-bound to records) | No | Yes |
| `docs/OPERATIONS.md` | Canonical: commands, test profiles, run rules | Source | No | Yes |
| `docs/HISTORY.md` | Canonical: milestones and the negative record | Source | No | Yes |
| `AGENTS.md` | Agent rules; `CLAUDE.md` imports it | Source | No | Yes |
| `docs/assets/paper/final/fig1…fig6*.svg`, `arch-motar-code-map.svg` (7) | The figures the README and site show; text stays text | Generated by `tools/build_paper_figures.py` | Yes, byte-exact with matplotlib 3.7.5 | Yes: GitHub and the site render them without a build step |
| `docs/assets/paper/final/manifest.json` | Values, source hashes and the hashes of all 21 renders, including the untracked PNG and PDF | Generated | Yes | Yes: it is the pin that `--check` and the tests verify |
| `docs/status/evidence.html` | The previous detailed page, moved to make room for the project page. Git records it as new because `index.html` was reused | Source | No | Yes |
| `docs/status/site.css` | Project-page styles | Source | No | Yes |
| `tools/build_paper_figures.py` | The one figure generator | Source | No | Yes |
| `tools/run_tests.py` | Test profiles `PUBLIC_CPU`, `GPU_REQUIRED`, `FULL_RESEARCH` | Source | No | Yes |
| `tests/test_canonical_docs.py` | Canonical-file, README and claim-consistency guards (P0 audit) | Source | No | Yes |
| `tests/test_project_page.py` | Project-page structure and wording guards | Source | No | Yes |
| `tests/test_paper_figures_final.py` | Figure values, boundaries and byte-exact rebuild | Source | No | Yes |
| `docs/cleanup/cleanup_record_2026-09-24.md` | The record of both passes and of the claim audit | Source | No | Yes, as the decision record |
| `docs/cleanup/new_file_budget.md` | This file | Source | No | Optional: the table could live only in the record |
| `docs/cleanup/preserved_contract_paths.md` | Paths that never move, with their enforcement; `AGENTS.md` links it | Source | No | Yes |
| `docs/cleanup/inventory_2026-09-24.json` | Census at the base commit plus a classification of every Markdown file | Generated by a one-off audit | No: no tool rebuilds it | Optional: it is a snapshot, kept because nothing else records the per-file decisions |
| `docs/d8c_perception_path_audit_2026-09-24.md` | Read-only D8c audit and the retraining decision | Source | No | Yes, as a research record |

**Not tracked, by design.** The 14 PNG and PDF renders in `docs/assets/paper/final/` are ignored by
`.gitignore`. Running `python tools/build_paper_figures.py` rebuilds them, and `manifest.json` pins
their hashes.

**Reader-facing canonical documents: 9.** They are `README.md`, `PROJECT.md`, `ARCHITECTURE.md`,
`DESIGN_SYSTEM.md`, `docs/README.md`, `docs/EVIDENCE.md`, `docs/REPRODUCIBILITY.md`,
`docs/OPERATIONS.md` and `docs/HISTORY.md`. `AGENTS.md` and `CLAUDE.md` are agent instructions.

## Set B — replication instrumentation and preregistration (5 files)

B is kept separate from A so that it can be reviewed and committed separately. The replication must
run from a clean tree on the commit that contains B.

| Path | Why it is tracked | Source or generated | Can it be regenerated? | Needed in Git? |
| --- | --- | --- | --- | --- |
| `docs/prereg_2026-09-24_target_motion_replication_6seed.md` | The preregistration; it must be committed before any cell runs | Source | No | Yes |
| `aerial_gym/task/navrl_task/navrl_episode_ledger.py` | Per-episode ledger and exact per-environment quota; off by default | Source | No | Yes |
| `tools/target_motion_replication.py` | Cell list, guarded launcher and analysis, fixed before data | Source | No | Yes |
| `tests/test_navrl_episode_ledger.py` | Quota exactness, unbiasedness, no RNG use, no input mutation, wiring | Source | No | Yes |
| `tests/test_target_motion_replication.py` | Design, exact-test resolution, BCa reproduction, analysis | Source | No | Yes |

B also edits two tracked files:
- `aerial_gym/task/navrl_task/navrl_task.py`: +16 lines, a guarded hook.
- `aerial_gym/rl_training/rl_games/runner.py`: one line, the player cap.
